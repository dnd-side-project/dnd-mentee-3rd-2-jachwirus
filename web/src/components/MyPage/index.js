import React, {Component} from 'react';

class MyPage extends Component {
    render() {
      return (
        <header>
          <h1>My Page</h1>
        </header>
      );
    }
  }

export default MyPage;