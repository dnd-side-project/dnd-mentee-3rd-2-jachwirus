import React, {Component} from 'react';

class HomePage extends Component {
    render() {
      return (
        <header>
          <h1>Home Page</h1>
        </header>
      );
    }
  }

export default HomePage;